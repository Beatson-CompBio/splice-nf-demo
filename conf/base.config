// Conservative defaults. Users can override with -profile.
params.outdir = params.outdir ?: "${projectDir}/results"
process {
  withName: '*'{
    cpus   = 1
    memory = '1 GB'
    time   = '2h'
  }
  // Caps
  maxForks = params.max_cpus as int
}
