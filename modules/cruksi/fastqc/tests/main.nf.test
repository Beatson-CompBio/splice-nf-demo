nextflow_process {

    name "Test Process FASTQC"
    script "../main.nf"
    process "FASTQC"

    tag "modules"
    tag "modules_cruksi"
    tag "fastqc"

    test("fastqc - single end") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:true ],
                    [ file('https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/illumina/amplicon/sample1_R1.fastq.gz', checkIfExists: true) ]
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.html.size() == 1 },
                { assert process.out.zip.size() == 1 },
                { assert process.out.versions.size() == 1 },
                // Only snapshot the deterministic outputs
                { assert snapshot(
                    process.out.html,
                    process.out.versions
                ).match() }
            )
        }
    }

    test("fastqc - paired end") {

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ],
                    [ 
                        file('https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/illumina/amplicon/sample1_R1.fastq.gz', checkIfExists: true),
                        file('https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/illumina/amplicon/sample1_R2.fastq.gz', checkIfExists: true)
                    ]
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.html.size() == 1 },
                { assert process.out.zip.size() == 1 },
                { assert process.out.versions.size() == 1 },
                // Only snapshot the deterministic outputs
                { assert snapshot(
                    process.out.html,
                    process.out.versions
                ).match() }
            )
        }
    }

    test("fastqc - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    [ id:'test', single_end:false ],
                    [ 
                        file('https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/illumina/amplicon/sample1_R1.fastq.gz', checkIfExists: true),
                        file('https://raw.githubusercontent.com/nf-core/test-datasets/viralrecon/illumina/amplicon/sample1_R2.fastq.gz', checkIfExists: true)
                    ]
                ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.html.size() == 1 },
                { assert process.out.zip.size() == 1 },
                { assert process.out.versions.size() == 1 }
                // No snapshot needed for stub - just check it runs
            )
        }
    }
}
